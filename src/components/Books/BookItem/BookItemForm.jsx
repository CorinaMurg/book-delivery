
import classes from './BookItemForm.module.css';

const BookItemForm = () => {
  return (
    <form className={classes.form}>
      <button>+ Add</button>
    </form>
  );
};

export default BookItemForm;